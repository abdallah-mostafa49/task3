#include <iostream>
#include <vector>
using namespace std;
int main()

{
      //---------------------
     // Answer question  1 :
    //----------------------

    // Define the length of the array

   /* const int length = 10; */


    //  Declare  array 

    /*int arr[length];*/




    // Take array values from the user

   /* cout << "Please enter " << length << " integers:" << endl;
    for (int i = 0; i < length; i++) {
        cout << "Enter element " << i + 1 << ": ";
        cin >> arr[i];
    }*/




    // Print the array elements separated by spaces
    /*cout << "\nArray elements:" << endl;
    for (int i = 0; i < length; i++) {
        cout << arr[i] << " ";
    }
    cout << endl;*/



    //  Print the array elements in reverse order separated by spaces
   /* cout << "\nArray elements in reverse order:" << endl;
    for (int i = length - 1; i >= 0; i--) {
        cout << arr[i] << " ";
    }
    cout << endl;*/



    // Calculate and print the summation of the array elements
    /*int summation = 0; 
    for (int i = 0; i < length; i++) {
        summation += arr[i];
    }
    cout << "\nSum of array elements: " << summation << endl;*/


    //Calculate and print the product of the array elements
   /* int product = 1;
    for (int i = 0; i < length; i++) {
        product *= arr[i];
    }
    cout << "Product of array elements: " << product << endl;*/

    //-------------------------------------------------------------


       //-------------------
      // Answer question  2 :
     //--------------------


   // Define the number of rows and columns
   /* const int rows = 3, columns = 3;*/

    //Define the 2 array with fixed size 3x3
   /* int matrix [3][3];*/

    //Take input values from the user

   /* cout << "Enter the values for the matrix (" << rows << "x" << columns << "):\n";
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < columns; j++)
        {
            cout << "Enter value for element [" << i << "][" << j << "]: ";
            cin >> matrix[i][j]; 
        }
    }*/

    //  Print the matrix with space between elements

   /* cout << "\nThe matrix is:\n";
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < columns; j++) {
            cout << matrix[i][j] << " ";  
        }
        cout << endl; 
    }*/

    //  Calculate and display the summation of each row

   /* cout << "\nSummation of each row:\n";
    for (int i = 0; i < rows; i++) {
        int rowSum = 0;
        for (int j = 0; j < columns; j++)
        {
            rowSum += matrix[i][j];   
        }
        cout << "Sum of row " << i + 1 << ": " << rowSum << endl;    
    }*/

    // Calculate and display the product of each row 

   /* cout << "\nProduct of each row:\n";
    for (int i = 0; i < rows; i++) {
        int rowProduct = 1;
        for (int j = 0; j < columns; j++) {
            rowProduct *= matrix[i][j];  
        }
        cout << "Product of row " << i + 1 << ": " << rowProduct << endl;
    }

}*/

//-------------------------------------------------------------------------------------------------------------